"use client";
import { Alert, AppBar, Badge, Button, Card, Chart, DataGrid, Divider, EmptyState, Progress, SearchForm, SpeedDial, Stack, Stepper, TextInput, Typography } from "@apexui/react";
type SupportRow = { signal: string; value: string; owner: string };
const columns: Array<{ key: keyof SupportRow; header: string }> = [{ key: "signal", header: "Signal" }, { key: "value", header: "Value" }, { key: "owner", header: "Owner" }];
const rows: SupportRow[] = [{ signal: "Static export", value: "Ready", owner: "Next" }, { signal: "SSR path", value: "React", owner: "App router" }, { signal: "Theme", value: "Graphite", owner: "Tokens" }];
export default function Page() { return <main className="demo-shell"><div className="demo-frame"><AppBar title="ApexUI Next Command" actions={<Button size="sm">Deploy</Button>} /><section className="demo-hero"><div className="demo-panel"><Stack gap="md"><Badge tone="info">Next.js static export</Badge><Typography as="h1" variant="display">Dark route, server-safe React components</Typography><Typography variant="body">This page uses the React package for app-router routes and exports cleanly to GitHub Pages.</Typography><SearchForm label="Command search" placeholder="Search routes" onSubmit={() => undefined} /><div className="demo-actions"><Button>Promote latest</Button><Button variant="secondary">Review tags</Button></div></Stack></div><Card eyebrow="Build" title="Pages output"><Progress label="Static readiness" value={91} /><Divider /><TextInput label="Base path" defaultValue="/apexui-demo-next" /></Card></section><section className="demo-grid"><Chart label="Route health" data={[{ label: "Build", value: 95 }, { label: "Types", value: 84 }, { label: "Pages", value: 90 }]} /><Stepper steps={[{ id: "install", label: "Install" }, { id: "build", label: "Build" }, { id: "deploy", label: "Deploy" }]} activeIndex={2} /><EmptyState title="No missing exports" description="All visible UI comes from ApexUI React components." action={<Button size="sm">Open package</Button>} /></section><section className="demo-workspace"><SpeedDial label="Actions" open actions={[{ id: "audit", label: "Audit" }, { id: "publish", label: "Publish" }, { id: "verify", label: "Verify" }]} /><DataGrid caption="Next.js support matrix" columns={columns} rows={rows} /></section><Alert tone="success" title="Next demo live">Static export is ready for GitHub Pages.</Alert></div></main>; }

